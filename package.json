{
  "name": "advanced-2fa-security-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Final-year MERN project: Advanced Two-Factor Authentication System with Intelligent and Invisible Security Mechanisms",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  }
}
